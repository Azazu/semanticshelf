# CLAUDE.md

@AGENTS.md

`AGENTS.md` is the single source of truth for repository guidance,
shared by all agents (Claude Code, Codex). Tool-specific details live in
its "Claude Code-Specific Notes" and "Codex-Specific Notes" sections.
Do not add guidance to this file — edit `AGENTS.md` instead; this shim
exists only because Claude Code reads `CLAUDE.md`, and the line above
imports it.
