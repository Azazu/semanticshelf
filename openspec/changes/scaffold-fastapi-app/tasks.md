## 1. Dependencies and settings

- [x] 1.1 Add the runtime dependencies (`fastapi`, `uvicorn[standard]`, `pydantic`, `pydantic-settings`, `sqlalchemy[asyncio]`, `asyncpg`, `alembic`, `structlog`) and dev `httpx` with `uv add` / `uv add --group dev`; commit `uv.lock`. Verify: `make lock-check` exits 0 and `uv run python -c 'import fastapi, sqlalchemy, alembic, structlog'` succeeds.
- [x] 1.2 Write `app/core/settings.py` (`Settings` on pydantic-settings: `database_url` required and validated to the `postgresql+asyncpg` scheme, `log_level`, `log_json`, `readiness_timeout_seconds`; `.env` file support; no module-level instance). Verify: unit tests in `tests/unit/test_settings.py` — missing `DATABASE_URL` raises a validation error naming the field; a `postgresql://` URL without the driver is rejected; defaults are as documented.

## 2. Logging, request id, errors

- [x] 2.1 Write `app/core/logging.py` (structlog configuration per design decision 5) and `app/core/request_id.py` (pure ASGI middleware per decision 3). Verify: api tests — a supplied valid `X-Request-ID` is echoed; an invalid one is replaced by a UUID4; the log line captured for the request carries `request_id`, `method`, `path` (structlog `capture_logs` or a captured stdout handler).
- [x] 2.2 Write `app/core/errors.py` (the `ProblemDetails` model, the validation and HTTP-exception handlers, the 500 body used by the middleware, per decision 4) and register the handlers in the factory. Verify: api tests — unknown route → 404 `application/problem+json` with `about:blank` and `instance` `urn:request:<id>`; a probe endpoint with a bad query parameter → 422 with `"type": "/errors/validation"` whose `errors[]` objects have exactly `loc`, `msg`, `type`; a test-only route raising `RuntimeError` → 500 with empty `detail`, no exception text in the body, and one error-level record with the traceback (client created with `raise_app_exceptions=False`).

## 3. Application, database, migrations

- [x] 3.1 Write `app/main.py` (`create_app`, lifespan with the engine, docs URLs, routers, middleware, handlers; started by uvicorn with `--factory`, no module-level instance — decision 1) and `app/db/engine.py` + `app/db/base.py` (engine factory, session maker, `get_session`, `Base` with naming conventions). Verify: api tests — `GET /api/openapi.json` is OpenAPI 3.1 with `info.version` equal to `app.__version__`; `GET /api/docs` is HTML; `GET /api/redoc` is 404; unit test that `Base.metadata.naming_convention` carries the five keys.
- [x] 3.2 Initialise Alembic with the classic async template (`uv run alembic init -t async alembic`), set `script_location`, `file_template = %%(rev)s_%%(slug)s` and no URL in `alembic.ini`; rewrite `alembic/env.py` to take the URL from `Settings` and `target_metadata` from `Base`; write revision `0001_baseline` (`CREATE EXTENSION IF NOT EXISTS vector` / `DROP EXTENSION IF EXISTS vector`). Verify: against a pgvector database (a throwaway container with trust auth on port 5434 was used, the repository having no `.env` at the time), `make migrate` applies it and `uv run alembic current` prints `0001_baseline (head)`; `uv run alembic downgrade base` then `make migrate` both succeed (recorded in the commit body).
- [x] 3.3 Write `app/api/health.py` and `app/services/readiness.py` (`GET /health`, `GET /ready`, the two checks per decision 6). Verify: api tests — `/health` is 200 with `status` `ok` and the package version without opening a connection (engine replaced by a stub that fails on connect); `/ready` with an unreachable URL is 503 with `checks.database` naming the cause and no `postgresql` substring in the body; unit tests of the aggregation with stubbed check results (all ok → 200 shape; one failing → 503 shape).

## 4. Test layers

- [x] 4.1 Write `tests/conftest.py` (`test_settings`, `app`, `client` fixtures with the lifespan run), `tests/api/__init__.py`, `tests/integration/__init__.py`; keep `tests/unit/test_package.py`. Verify: `make test` runs the unit and api layers without any database variable set in the environment (`env -u DATABASE_URL make test`) and passes.
- [x] 4.2 Write `tests/integration/test_readiness.py` (marked `integration`): with `DATABASE_URL` set and the database migrated, `/ready` is 200 with both checks `ok`; after `alembic downgrade base` executed by the test in a subprocess on the same URL (Alembic's async env calls `asyncio.run`, which cannot nest in the test's loop), `/ready` is 503 with `checks.migrations` naming the expected head, and the test upgrades back to head in `finally`. Verify: `make test-integration` passes against the throwaway pgvector database of task 3.2; the test is skipped with a clear message when `DATABASE_URL` is unset.

## 5. Quality floor and guard inertness

- [x] 5.1 `make check` green (`lock-check`, ruff, format, mypy strict over the new modules, unit and api tests). Verify: `check: all green` recorded in the commit body; mypy runs with `--strict` on `app/` including the new packages (no `type: ignore` without a reason comment).
- [x] 5.2 Format and lint the generated Alembic files too (`alembic/` is inside ruff's paths). Verify: `make lint` and `make fmt-check` report no findings under `alembic/`.
- [x] 5.3 Guard inertness: `make migrate`, `make run` (started with a 3-second timeout), `make test-integration` no longer print SKIP; `make help` unchanged. Verify: outputs recorded in the commit body; `make run` answers `curl -s http://127.0.0.1:8000/health` with the version (recorded).

## 6. Documentation and environment

- [x] 6.1 Write `docs/reference/settings.md` (variable, default, required, read by whom, example line) and link it from `docs/README.md`; update `docs/how-to/local-development.md` (retire the "Before the application exists" section, name `/health`, `/ready`, the Swagger UI path, readiness semantics, the extension privilege note), `docs/reference/commands.md` (guard note: the targets are real now; the guard text stays for a fresh clone before migrations only if still true — else remove), `README.md` (stage `scaffolded`, quick start URLs). Verify: re-read all five whole; every command added by this change (`uv add …`, `uv run alembic init -t async alembic`, `uv run alembic current`, `uv run alembic downgrade base`, `env -u DATABASE_URL make test`, `curl -s http://127.0.0.1:8000/health`) was run in its exact form in tasks 1–5 with the evidence in the commit bodies named in 7.1.
- [x] 6.2 Environment template (user action): the executor prints the exact lines `docs/reference/settings.md` prescribes for the template; the user checks the template and adds or renames lines as needed, then runs `make run` and `curl -s http://127.0.0.1:8000/ready`. Verify: the user reports `{"status":"ready",...}`.

## 7. Wrap-up

- [x] 7.1 Commits per block, without agent trailers: `chore(deps): add the application runtime dependencies` (1.1), `feat(core): settings, structured logging, request id and problem details` (1.2, 2.1, 2.2 with their unit tests; the api tests of 2.1–2.2 need the factory and land with it), `feat(app): application factory, database wiring, alembic baseline and probes` (3.1–3.3, 4.1, 4.2, the api tests, the Makefile `run` target; body: 3.2, 4.1, 5.1, 5.3 evidence), `docs: describe the scaffolded application` (6.1). Verify: `git log --format=%B main..HEAD | grep -c '^Co-Authored-By'` prints `0`.
- [x] 7.2 Acceptance on GitHub (user pushes the branch): `workflow` green, `python` green with the integration step running `make migrate` and the marked tests against the service. Verify: the user reports the run.
- [x] 7.3 `openspec validate scaffold-fastapi-app --strict` and `scripts/pregate-verify.sh gate2 scaffold-fastapi-app` pass; request Gate 2 with `/gate-review scaffold-fastapi-app 2`.
