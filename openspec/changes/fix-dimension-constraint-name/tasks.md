## 1. The revision

- [ ] 1.1 Write the revision under `alembic/versions/` on top of `0002_asset_schema`: it renames the nine check constraints to the names the metadata declares (six on `assets`, one on `embeddings`, two on `indexing_jobs`), with the name pairs written out in the file rather than imported or computed (design decision 2), and a downgrade that renames them back. Verify: `make migrate` applies it against the integration database and `uv run alembic heads` names the new revision.
- [ ] 1.2 Reversibility by hand, once: `uv run alembic downgrade -1` restores the doubled names and `uv run alembic upgrade head` restores the declared ones. Verify: the catalog listing in both states recorded in the commit body.

## 2. The guard that would have caught it

- [ ] 2.1 Write `tests/integration/test_schema_names.py`: for every table the application owns, the set of constraint names read from the catalog equals the set the metadata declares — a set comparison, so an extra constraint fails it as loudly as a missing one (design decision 4). Verify: `make test-integration` green against a database at head.
- [ ] 2.2 Demonstrate the guard fails on the schema it is meant to catch: with the revision downgraded, it reports the nine doubled names. Verify: output recorded in the commit body, the database returned to head afterwards.

## 3. The claims the old names left behind

- [ ] 3.1 Make the dimension assertion in `tests/integration/test_embedding_storage.py` exact, so it can no longer pass against a doubled name by substring. Verify: it passes at head, and fails with the revision downgraded; both outputs recorded.
- [ ] 3.2 Correct row 4a in `openspec/ROADMAP.md`: it claims an autogenerated revision would propose dropping and recreating the constraint, and an autogenerate probe against the database at head produced an empty upgrade. Verify: the corrected row re-read whole; `rg -n 'ck_embeddings_ck_embeddings' --glob '!openspec/changes/**' .` returns only `alembic/versions/` history and the new revision.
- [ ] 3.3 Confirm ADR-001 needs no edit: it already names the constraint in the declared form, which the rename makes true. Verify: the ADR line re-read and quoted in the commit body; no ADR is edited (they are append-only).

## 4. The one caller that reads constraints

- [ ] 4.1 Confirm the readiness probe is unaffected in both directions: it reads the dimension constraint by table, not by name. Verify: `make test-integration` includes the readiness tests green after the rename, and `GET /ready` against the integration database answers 200 with three `ok` checks, recorded in the commit body.

## 5. Wrap-up

- [ ] 5.1 Commits per logical block, no agent trailers, each body naming the evidence for the tasks its diff holds. Verify: `git log --format=%B main..HEAD | grep -c '^Co-Authored-By'` prints `0`.
- [ ] 5.2 `env -u DATABASE_URL make check` green and `make test-integration` green against pgvector at head. Verify: both outputs recorded in a commit body.
- [ ] 5.3 Acceptance on GitHub (the user pushes the branch): `workflow` and `python` jobs green, the migration applied in CI before the integration suite. Verify: the user reports the run.
- [ ] 5.4 `openspec validate fix-dimension-constraint-name --strict` and `scripts/pregate-verify.sh gate2 fix-dimension-constraint-name` pass; request Gate 2 with `/gate-review fix-dimension-constraint-name 2`.
