# Handoff — fix-dimension-constraint-name

**Updated:** 2026-09-21 · claude
**State:** proposing
**Branch:** change/fix-dimension-constraint-name

## Done this session

- Branch and scaffold created. The work: rename the dimension constraint on
  `embeddings` to the name the ORM metadata declares. Migration 0002 passed a
  name that already carried the `ck_embeddings_` prefix and the naming
  convention added it again, so the database holds
  `ck_embeddings_ck_embeddings_model_dimension` while
  `app/models/embedding.py` declares `ck_embeddings_model_dimension`. Nothing
  is broken today — the readiness check reads the constraint by table, not by
  name — but an autogenerated revision will propose dropping and recreating
  it inside some unrelated change.

## Next step

`/opsx:apply fix-dimension-constraint-name`. Tier is medium, so there is no
Gate 1: implementation may start, and Gate 2 follows the user's push.

## Blockers

None.
